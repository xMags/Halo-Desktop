Run `tools/fetch-mpv.ps1` to produce the ignored libmpv headers, DLL, definition file, and import library.
The fetcher selects the baseline x64 LGPL build from zhongfly/mpv-winbuild. The generated `external/mpv/VERSION.txt` records the exact fetched release.
